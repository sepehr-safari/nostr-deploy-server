# Server Configuration
PORT=3000
NODE_ENV=development

# Domain Configuration
BASE_DOMAIN=example.com
WILDCARD_CERT_PATH=/path/to/wildcard/cert.pem
WILDCARD_KEY_PATH=/path/to/wildcard/key.pem

# Default Nostr Relays (comma-separated)
DEFAULT_RELAYS=wss://nos.lol,wss://ditto.pub/relay,wss://relay.damus.io

# Default Blossom Servers (comma-separated)
DEFAULT_BLOSSOM_SERVERS=https://cdn.hzrd149.com,https://nostr.download

# Caching Configuration
CACHE_TTL_SECONDS=300
MAX_CACHE_SIZE=100

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FORMAT=combined

# Security
CORS_ORIGIN=*
TRUST_PROXY=false

# Performance
REQUEST_TIMEOUT_MS=30000
MAX_FILE_SIZE_MB=50 